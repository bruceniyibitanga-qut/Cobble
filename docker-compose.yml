services:
  # Database (MySQL 8.4 LTS) — schema.sql is applied automatically on first run
  db:
    image: mysql:8.4
    container_name: cobble-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: qut_crm
      MYSQL_USER: qut_crm
      MYSQL_PASSWORD: ${DB_PASSWORD:-changeme_local_only}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-changeme_local_only_root}
    ports:
      - "3306:3306"
    volumes:
      - cobble-dbdata:/var/lib/mysql
      - ./Schema/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "qut_crm", "-p${DB_PASSWORD:-changeme_local_only}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - cobble-network

  # API (ASP.NET Core / C#)
  api:
    build:
      context: ./api/CobbleAPI
      dockerfile: CobbleAPI/Dockerfile
    container_name: cobble-api
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__Default=Server=db;Port=3306;Database=qut_crm;User=qut_crm;Password=${DB_PASSWORD:-changeme_local_only}
      - Jwt__Issuer=CobbleAPI
      - Jwt__Audience=CobbleFrontend
      - Jwt__ExpiryInMinutes=60
      # JWT keys are loaded from appsettings.json in dev.
      # In production, override Jwt__PrivateKeyPem and Jwt__PublicKeyPem via environment or Docker secrets.
    networks:
      - cobble-network

  # Adminer — lightweight web UI for browsing the database
  adminer:
    image: adminer:latest
    container_name: cobble-adminer
    restart: unless-stopped
    ports:
      - "5050:8080"
    environment:
      ADMINER_DEFAULT_SERVER: db
    depends_on:
      db:
        condition: service_healthy
    networks:
      - cobble-network

volumes:
  cobble-dbdata:

networks:
  cobble-network:
    driver: bridge